'use strict';
function escapeHtml(value) { return String(value).replace(/[&<>"]/g, (ch) => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[ch])); }
function formatNumber(value) { return Number.isFinite(value) ? value.toFixed(2) : ''; }
export function renderHtmlDocument(viewModel) {
  const rows = viewModel.rows.map((row) => `<tr data-path="${escapeHtml(row.path)}" data-classification="${row.classification}"><td>${escapeHtml(row.path)}</td><td>${escapeHtml(row.classification)}</td><td>${escapeHtml(row.layer)}</td><td>${escapeHtml(row.rule)}</td><td>${formatNumber(row.spTotalScore)}</td><td>${formatNumber(row.rawScore)}</td><td>${formatNumber(row.finalScore)}</td></tr>`).join('');
  return `<!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>E2R V2 Phase 1</title><style>body{font-family:system-ui,sans-serif;margin:0;background:linear-gradient(180deg,#151922,#0d1117);color:#e5e7eb}header{padding:20px;border-bottom:1px solid #334155;background:#0b1220;position:sticky;top:0}.pill{display:inline-block;padding:4px 10px;border-radius:999px;background:#1f2937;margin-right:8px}.search{width:100%;padding:12px;border-radius:8px;border:1px solid #334155;background:#0b1220;color:#e5e7eb}table{width:100%;border-collapse:collapse;background:#0b1220;border:1px solid #334155}th,td{padding:8px 10px;border-bottom:1px solid #1f2937;text-align:left}</style></head><body><header><span class="pill">${escapeHtml(viewModel.phaseLabel)}</span><span class="pill">${escapeHtml(viewModel.capabilityLabel)}</span><span class="pill">${escapeHtml(viewModel.runtimeLabel)}</span></header><main style="padding:20px"><input id="search" class="search" placeholder="Search full path"><table id="members"><thead><tr><th>Path</th><th>Class</th><th>Layer</th><th>Rule</th><th>SP</th><th>Raw</th><th>Final</th></tr></thead><tbody>${rows}</tbody></table><pre>${escapeHtml(JSON.stringify(viewModel.summary, null, 2))}</pre><script type="module">${viewModel.clientScript || ''}</script></main></body></html>`;
}
